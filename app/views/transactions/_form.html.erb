<%= form_with(model: transaction, local: true) do |form| %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, class: "form-input" %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, class: "form-input" %>
  </div>
  
  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, class: "form-input" %>
  </div>
  
   <fieldset>
    <div class="form-radio-btn-item">
    <div class="card-body-rad form-inline card-radio-btn">
        <%=form.radio_button :group_id, 0 %>
        <label for = "0" class: "card-radio-btn-item"> NO GROUP </label>
    </div>
    <% @groups.each do |group|%>
      <div class="card-body-rad form-inline card-radio-btn">
        <%=form.radio_button :group_id, group.id %>
        <label for = "<%group.id%>" class: "card-radio-btn-item"> &nbsp<%= image_tag  "#{group.icon}", class: "card-img" %>&nbsp&nbsp<%= group.name %> </label>
      </div>
    <%end%>
    </div>
  </fieldset>


  <div class="actions">
    <%= form.submit class: "form-input"%>
  </div>
<% end %>
